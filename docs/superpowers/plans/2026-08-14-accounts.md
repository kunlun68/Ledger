# 多账户 + 转账 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 支持多账户（现金/微信/支付宝/银行卡）与账户间转账，首页显示总资产，统计自动排除转账。

**Architecture:** accounts 新表 + transactions 加 accountId/transferAccountId（categoryId 改可空）+ TxType.transfer；转账单条双向记录；`accountBalances` 纯函数聚合余额；AccountPicker/TransferScreen/AccountsScreen 三个新 UI；首页总资产。

**Tech Stack:** drift TableMigration 重建 + 现有 riverpod 栈，零新依赖。

**Spec:** docs/superpowers/specs/2026-08-14-accounts-design.md

## Global Constraints

- 金额一律整数分；日期 int yyyyMMdd
- 转账 type=transfer、categoryId=null；收支统计只算 expense/income（transfer 天然排除）
- 账户余额 = Σ收入 − Σ支出 + Σ转入 − Σ转出
- TDD：先写失败测试 → 确认红 → 实现 → 绿 → commit
- drift widget 测试末尾必须：`await tester.pumpWidget(const SizedBox()); await tester.pump(Duration.zero);`；内存 db + UncontrolledProviderScope container 模式；测试内禁用 `await stream.first`（fake-async 死锁），用一次性查询
- bash 调用需导出：`export PATH="/c/src/flutter/bin:$PATH" PUB_HOSTED_URL="https://pub.flutter-io.cn" FLUTTER_STORAGE_BASE_URL="https://storage.flutter-io.cn"`
- 改 tables.dart 后必须重跑：`dart run build_runner build --delete-conflicting-outputs`
- commit message 简洁英文 + `Co-Authored-By: Claude <noreply@anthropic.com>`

---

### Task 1: schema v4 + migration + 全库编译修复

**Files:**
- Modify: `lib/data/tables.dart`（Accounts 表；Transactions 三改动；RecurringRules 加 accountId）
- Modify: `lib/data/transaction_type.dart`（TxType 加 transfer）
- Modify: `lib/data/database.dart`（表注册、schemaVersion 4、onUpgrade）
- Modify: `test/data/migration_test.dart`（追加 v3→v4）
- **连带编译修复**（categoryId 可空 + Transaction 新字段）：
  - `lib/data/dao/transactions_dao.dart`：insertTransaction 加 `required int accountId`；updateTransaction 加 `int? accountId`；新增 insertTransfer（Task 4 定义，本任务先加编译占位——见下）
  - `lib/data/dao/recurring_dao.dart`：insertRule 加 `required int accountId`；generateDue 插入用 r.accountId
  - `lib/core/stats.dart`：expenseByCategory 跳过 categoryId==null
  - `lib/presentation/screens/*`：catName 签名改 `int?`（null→'未分类'）；add_transaction_screen `_save` 暂传 accountId: 1
  - 测试手动构造 Transaction/Category 处补 `accountId: 1`：test/core/backup_test.dart、test/core/csv_export_test.dart、test/presentation/settings_screen_test.dart
  - recurring_dao_test/settings 等调用 insertTransaction/insertRule 处补 accountId

**Interfaces:**
- Produces: `Account` 数据类（id/name/icon/sortOrder/createdAt）；`Transaction.accountId`（int）、`Transaction.transferAccountId`（int?）、`Transaction.categoryId`（int?）；`RecurringRule.accountId`（int）；`TxType.transfer`

- [ ] **Step 1: 写失败测试**

```dart
// test/data/migration_test.dart main() 内追加：
  test('migration from v3 keeps data, creates default account', () async {
    final dir = Directory.systemTemp.createTempSync('ledger_migration3');
    final file = File('${dir.path}${Platform.pathSeparator}test.db');
    addTearDown(() {
      dir.deleteSync(recursive: true);
    });

    // 手写 v3 schema（transactions 无 account 列、category_id NOT NULL；recurring_rules 无 account_id）
    final v3 = AppDatabase.open(executor: NativeDatabase(file));
    await v3.customStatement('DROP TABLE IF EXISTS recurring_rules');
    await v3.customStatement('DROP TABLE IF EXISTS transactions');
    await v3.customStatement('DROP TABLE IF EXISTS categories');
    await v3.customStatement('''
CREATE TABLE categories (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  icon TEXT NOT NULL,
  type TEXT NOT NULL,
  sort_order INTEGER NOT NULL DEFAULT 0,
  is_builtin INTEGER NOT NULL DEFAULT 0,
  monthly_budget_cents INTEGER NOT NULL DEFAULT 0
)''');
    await v3.customStatement('''
CREATE TABLE transactions (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  type TEXT NOT NULL,
  amount_cents INTEGER NOT NULL,
  category_id INTEGER NOT NULL REFERENCES categories(id),
  note TEXT NOT NULL DEFAULT '',
  date INTEGER NOT NULL,
  created_at INTEGER NOT NULL DEFAULT 0,
  updated_at INTEGER NOT NULL DEFAULT 0
)''');
    await v3.customStatement('''
CREATE TABLE recurring_rules (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  type TEXT NOT NULL,
  amount_cents INTEGER NOT NULL,
  category_id INTEGER NOT NULL REFERENCES categories(id),
  note TEXT NOT NULL DEFAULT '',
  day_of_month INTEGER NOT NULL,
  last_generated_yyyymm INTEGER NOT NULL DEFAULT 0,
  created_at INTEGER NOT NULL DEFAULT 0
)''');
    await v3.customStatement(
        "INSERT INTO categories (name, icon, type, sort_order, is_builtin) "
        "VALUES ('餐饮', 'restaurant', 'expense', 0, 1)");
    await v3.customStatement(
        "INSERT INTO transactions (type, amount_cents, category_id, note, date) "
        "VALUES ('expense', 1234, 1, '午饭', 20260813)");
    await v3.customStatement('PRAGMA user_version = 3');
    await v3.close();

    final db = AppDatabase.open(executor: NativeDatabase(file));
    addTearDown(db.close);
    final accounts = await db.select(db.accounts).get();
    expect(accounts.single.name, '现金'); // 默认账户
    final txs = await db.select(db.transactions).get();
    expect(txs.single.accountId, accounts.single.id); // 旧交易归默认账户
    expect(txs.single.categoryId, 1);
    expect(await db.select(db.recurringRules).get(), isEmpty);
  });
```

- [ ] **Step 2: 运行确认失败**

Run: `flutter test test/data/migration_test.dart`
Expected: FAIL — 编译错误（db.accounts 不存在）

- [ ] **Step 3: 实现 schema 与 migration**

```dart
// lib/data/transaction_type.dart
enum TxType { expense, income, transfer }
```

```dart
// lib/data/tables.dart
class Accounts extends Table {
  IntColumn get id => integer().autoIncrement()();
  TextColumn get name => text().withLength(min: 1, max: 20)();
  TextColumn get icon => text().withLength(min: 1, max: 50)();
  IntColumn get sortOrder => integer().withDefault(const Constant(0))();
  DateTimeColumn get createdAt => dateTime().withDefault(currentDateAndTime)();
}
// Transactions 内 categoryId 改为：
  IntColumn get categoryId => integer().nullable().references(Categories, #id)();
// Transactions 内 date 之后加：
  IntColumn get accountId => integer().references(Accounts, #id).withDefault(const Constant(1))();
  IntColumn get transferAccountId => integer().nullable().references(Accounts, #id)();
// RecurringRules 内 categoryId 之后加：
  IntColumn get accountId => integer().references(Accounts, #id).withDefault(const Constant(1))();
```

```dart
// lib/data/database.dart
@DriftDatabase(tables: [Categories, Transactions, RecurringRules, Accounts])
// schemaVersion => 4
        onUpgrade: (m, from, to) async {
          if (from < 2) {
            await m.addColumn(categories, categories.monthlyBudgetCents);
          }
          if (from < 3) {
            await m.createTable(recurringRules);
          }
          if (from < 4) {
            await m.createTable(accounts);
            await into(accounts).insert(const AccountsCompanion.insert(
                name: '现金', icon: 'payments', sortOrder: Value(0)));
            // 用最新表定义重建 transactions（加 account 列 + category_id 可空），旧数据自动补 DEFAULT 1
            await m.alterTable(TableMigration(transactions));
            await m.addColumn(recurringRules, recurringRules.accountId);
          }
        },
```

重新生成：`dart run build_runner build --delete-conflicting-outputs`

- [ ] **Step 4: 连带编译修复（逐文件，改动机械）**

```dart
// lib/data/dao/transactions_dao.dart
  Future<void> insertTransaction(TxType type, int amountCents, int? categoryId,
          int date, String note, {required int accountId}) =>
      db.into(db.transactions).insert(TransactionsCompanion.insert(
            type: type,
            amountCents: amountCents,
            categoryId: Value(categoryId),
            accountId: accountId,
            date: date,
            note: Value(note),
          ));
  // updateTransaction 加 {int? accountId} → copyWith(accountId: accountId ?? t.accountId)
```

```dart
// lib/data/dao/recurring_dao.dart
  // insertRule 加 required int accountId → companion accountId: accountId
  // generateDue 插入 transactions 时 accountId: r.accountId
```

```dart
// lib/core/stats.dart expenseByCategory：
    for (final t in txs.where((t) => t.type == TxType.expense)) {
      final cid = t.categoryId;
      if (cid == null) continue;
      map[cid] = (map[cid] ?? 0) + t.amountCents;
    }
```

```dart
// 所有 catName 调用方（home_screen/transactions_screen/statistics_screen/recurring_screen）：
  String catName(int? id) => id == null
      ? '未分类'
      : cats.where((c) => c.id == id).map((c) => c.name).firstOrNull ?? '未分类';
```

```dart
// lib/presentation/screens/add_transaction_screen.dart _save：
//   insertTransaction(..., accountId: 1)（Task 5 换成选择器）
```

```dart
// 测试补 accountId：
// - backup_test._tx / settings_screen_test 构造 Transaction：加 accountId: 1
// - recurring_dao_test、transactions_dao_test、add/home/transactions/statistics 测试
//   所有 insertTransaction 调用加 accountId: 1；insertRule 调用加 accountId: 1
```

- [ ] **Step 5: 运行确认通过**

Run: `flutter test`
Expected: 全部 PASS（原 81 + 新 migration 1 = 82 tests）

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat: add accounts table and transfer fields with v4 migration"
```

---

### Task 2: 余额聚合纯函数

**Files:**
- Modify: `lib/core/stats.dart`（+accountBalances/totalAssets）
- Modify: `test/core/stats_test.dart`（追加）

**Interfaces:**
- Produces:
  - `Map<int, int> accountBalances(List<Transaction> txs)` — accountId → 余额（分）
  - `int totalAssets(List<Transaction> txs)` — 全部余额合计

- [ ] **Step 1: 写失败测试**

```dart
// test/core/stats_test.dart 追加：
  Transaction tx(int id, TxType type, int cents, int account, {int? to}) => Transaction(
      id: id, type: type, amountCents: cents, categoryId: type == TxType.transfer ? null : 1,
      note: '', date: 20260801, accountId: account, transferAccountId: to,
      createdAt: DateTime(2026, 8, 1), updatedAt: DateTime(2026, 8, 1));

  test('accountBalances sums income/expense and moves transfers', () {
    final txs = [
      tx(1, TxType.income, 10000, 1), // 账户 1 +100
      tx(2, TxType.expense, 3000, 1), // 账户 1 -30
      tx(3, TxType.transfer, 2000, 1, to: 2), // 1→2 转 20
      tx(4, TxType.income, 500, 3), // 账户 3 +5
    ];
    final b = accountBalances(txs);
    expect(b[1], 10000 - 3000 - 2000);
    expect(b[2], 2000);
    expect(b[3], 500);
    expect(totalAssets(txs), 10000 - 3000 + 500);
  });
```

- [ ] **Step 2: 运行确认失败** → **Step 3: 实现**

```dart
// lib/core/stats.dart 末尾追加：
/// accountId → 余额（分）。expense/income 按账户归属，transfer 双向计入。
Map<int, int> accountBalances(List<Transaction> txs) {
  final map = <int, int>{};
  for (final t in txs) {
    switch (t.type) {
      case TxType.expense:
        map[t.accountId] = (map[t.accountId] ?? 0) - t.amountCents;
      case TxType.income:
        map[t.accountId] = (map[t.accountId] ?? 0) + t.amountCents;
      case TxType.transfer:
        map[t.accountId] = (map[t.accountId] ?? 0) - t.amountCents;
        final to = t.transferAccountId;
        if (to != null) map[to] = (map[to] ?? 0) + t.amountCents;
    }
  }
  return map;
}

/// 总资产：全部账户余额合计（分）。
int totalAssets(List<Transaction> txs) =>
    accountBalances(txs).values.fold(0, (a, b) => a + b);
```

- [ ] **Step 4: 运行确认通过**

Run: `flutter test test/core/stats_test.dart`
Expected: PASS（3 + 1 = 4 tests）

- [ ] **Step 5: 提交**

```bash
git add lib/core/stats.dart test/core/stats_test.dart
git commit -m "feat: add account balance aggregation"
```

---

### Task 3: AccountsDao + 转账插入

**Files:**
- Create: `lib/data/dao/accounts_dao.dart`
- Create: `test/data/accounts_dao_test.dart`
- Modify: `lib/data/dao/transactions_dao.dart`（+insertTransfer）

**Interfaces:**
- Consumes: Task 1 `Account`/`Transaction` 新字段、Task 2 `accountBalances`
- Produces:
  - `class AccountInUseException implements Exception {}`
  - `class AccountsDao { Future<void> insertAccount(String name, String icon); Future<void> deleteAccount(Account a); Stream<List<Account>> watchAll(); Future<int> balanceOf(int accountId); }`
  - `Future<void> insertTransfer({required int fromAccountId, required int toAccountId, required int amountCents, required int date, String note = ''})` — TransactionsDao 方法

- [ ] **Step 1: 写失败测试**

```dart
// test/data/accounts_dao_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:drift/native.dart';
import 'package:ledger/data/dao/accounts_dao.dart';
import 'package:ledger/data/dao/categories_dao.dart';
import 'package:ledger/data/dao/transactions_dao.dart';
import 'package:ledger/data/database.dart';
import 'package:ledger/data/transaction_type.dart';

void main() {
  late AppDatabase db;
  late AccountsDao dao;

  setUp(() async {
    db = AppDatabase.open(executor: NativeDatabase.memory());
    dao = AccountsDao(db);
    await db.into(db.accounts).insert(
        const AccountsCompanion.insert(name: '现金', icon: 'payments', sortOrder: Value(0)));
  });
  tearDown(() => db.close());

  test('insert and watchAll', () async {
    await dao.insertAccount('微信', 'wechat');
    final accounts = await (await dao.watchAll().first)
        .map((a) => a.name)
        .toList(); // 此处非 widget 测试，stream.first 安全
    expect(accounts, containsAll(['现金', '微信']));
  });

  test('balanceOf reflects income expense and transfers', () async {
    await CategoriesDao(db).seedBuiltinCategories();
    final food = (await CategoriesDao(db).getByType(TxType.expense)).first.id;
    await TransactionsDao(db)
        .insertTransaction(TxType.income, 10000, null, 20260801, '初始', accountId: 1);
    await TransactionsDao(db)
        .insertTransaction(TxType.expense, 3000, food, 20260802, '', accountId: 1);
    await dao.insertAccount('微信', 'wechat');
    final wxId = (await db.select(db.accounts).get())
        .firstWhere((a) => a.name == '微信')
        .id;
    await TransactionsDao(db).insertTransfer(
        fromAccountId: 1, toAccountId: wxId, amountCents: 2000, date: 20260803);
    expect(await dao.balanceOf(1), 10000 - 3000 - 2000);
    expect(await dao.balanceOf(wxId), 2000);
  });

  test('deleteAccount throws when account has transactions', () async {
    await TransactionsDao(db)
        .insertTransaction(TxType.expense, 100, 1, 20260801, '', accountId: 1);
    final cash = (await db.select(db.accounts).get()).single;
    expect(() => dao.deleteAccount(cash), throwsA(isA<AccountInUseException>()));
  });

  test('deleteAccount works when empty', () async {
    await dao.insertAccount('微信', 'wechat');
    final wx = (await db.select(db.accounts).get()).firstWhere((a) => a.name == '微信');
    await dao.deleteAccount(wx);
    expect((await db.select(db.accounts).get()).map((a) => a.name), ['现金']);
  });
}
```

- [ ] **Step 2: 运行确认失败** → **Step 3: 实现**

```dart
// lib/data/dao/accounts_dao.dart
import 'package:drift/drift.dart';
import '../../core/stats.dart';
import '../database.dart';

/// 删除被交易或转账引用的账户时抛出
class AccountInUseException implements Exception {}

class AccountsDao {
  AccountsDao(this.db);
  final AppDatabase db;

  Future<void> insertAccount(String name, String icon) =>
      db.into(db.accounts).insert(AccountsCompanion.insert(name: name, icon: icon));

  Future<void> deleteAccount(Account a) async {
    final used = await (db.select(db.transactions)
          ..where((t) => t.accountId.equals(a.id) | t.transferAccountId.equals(a.id)))
        .get();
    if (used.isNotEmpty) throw AccountInUseException();
    await (db.delete(db.accounts)..where((t) => t.id.equals(a.id))).go();
  }

  Stream<List<Account>> watchAll() => (db.select(db.accounts)
        ..orderBy([(t) => OrderingTerm.asc(t.sortOrder), (t) => OrderingTerm.asc(t.id)]))
      .watch();

  Future<int> balanceOf(int accountId) async {
    final txs = await db.select(db.transactions).get();
    return accountBalances(txs)[accountId] ?? 0;
  }
}
```

```dart
// lib/data/dao/transactions_dao.dart 追加：
  /// 转账：单条双向记录（type=transfer, account=转出, transferAccount=转入, category=null）。
  Future<void> insertTransfer(
          {required int fromAccountId,
          required int toAccountId,
          required int amountCents,
          required int date,
          String note = ''}) =>
      db.into(db.transactions).insert(TransactionsCompanion.insert(
            type: TxType.transfer,
            amountCents: amountCents,
            categoryId: const Value(null),
            accountId: fromAccountId,
            transferAccountId: Value(toAccountId),
            date: date,
            note: Value(note),
          ));
```

- [ ] **Step 4: 运行确认通过**

Run: `flutter test test/data/accounts_dao_test.dart`
Expected: PASS（4 tests）。注意 transfer_account_id 为 null 的 where 匹配（`t.transferAccountId.equals(a.id)` 对 null 行不匹配，语义正确）。

- [ ] **Step 5: 提交**

```bash
git add lib/data/dao/accounts_dao.dart test/data/accounts_dao_test.dart lib/data/dao/transactions_dao.dart
git commit -m "feat: add accounts dao with balance and transfer insert"
```

---

### Task 4: providers + 记一笔账户选择器

**Files:**
- Modify: `lib/application/providers.dart`（+accountsDaoProvider/allAccountsProvider/totalAssetsProvider）
- Create: `lib/presentation/widgets/account_picker.dart`
- Modify: `lib/presentation/screens/add_transaction_screen.dart`（账户选择器 + 保存带 accountId）
- Modify: `test/presentation/add_transaction_screen_test.dart`（断言 accountId）

**Interfaces:**
- Produces: `accountsDaoProvider`、`allAccountsProvider`（StreamProvider<List<Account>>）、`totalAssetsProvider`（Provider<int>）；`class AccountPicker extends ConsumerWidget { final int selectedId; final ValueChanged<int> onSelect; }`

- [ ] **Step 1: 写失败测试**

```dart
// test/presentation/add_transaction_screen_test.dart 现有保存测试的断言追加：
    expect(txs.single.accountId, 1); // 默认账户
// 追加测试：
  testWidgets('shows account picker and saves with selected account', (tester) async {
    // 加第二个账户
    final accountsDao = AccountsDao(db);
    await accountsDao.insertAccount('微信', 'wechat');
    final wxId = (await db.select(db.accounts).get()).firstWhere((a) => a.name == '微信').id;
    await pumpForm(tester);
    await tester.tap(find.text('微信'));
    await tester.pumpAndSettle();
    await tester.enterText(find.byType(TextField).first, '5.00');
    await tester.tap(find.text('保存'));
    await tester.pumpAndSettle();
    final txs = await TransactionsDao(db).getByMonth(yyyymmOf(todayYyyymmdd()));
    expect(txs.single.accountId, wxId);
    await tester.pumpWidget(const SizedBox());
    await tester.pump(Duration.zero);
  });
```

- [ ] **Step 2: 运行确认失败** → **Step 3: 实现**

```dart
// lib/application/providers.dart
import '../data/dao/accounts_dao.dart';
// 末尾：
final accountsDaoProvider = Provider((ref) => AccountsDao(ref.watch(databaseProvider)));
final allAccountsProvider = StreamProvider<List<Account>>(
    (ref) => ref.watch(accountsDaoProvider).watchAll());
final totalAssetsProvider = Provider<int>((ref) {
  final txs = ref.watch(allTransactionsProvider).value ?? const <Transaction>[];
  return totalAssets(txs);
});
```

```dart
// lib/presentation/widgets/account_picker.dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import '../../application/providers.dart';
import '../../data/database.dart';

/// 账户横向选择器（样式同 CategoryPicker）。
class AccountPicker extends ConsumerWidget {
  const AccountPicker({super.key, required this.selectedId, required this.onSelect});

  final int selectedId;
  final ValueChanged<int> onSelect;

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final accounts = ref.watch(allAccountsProvider).value ?? const <Account>[];
    final colorScheme = Theme.of(context).colorScheme;
    return SizedBox(
      height: 56,
      child: ListView.builder(
        scrollDirection: Axis.horizontal,
        itemCount: accounts.length,
        itemBuilder: (_, i) {
          final a = accounts[i];
          final selected = a.id == selectedId;
          return InkWell(
            onTap: () => onSelect(a.id),
            borderRadius: BorderRadius.circular(12),
            child: Container(
              margin: const EdgeInsets.symmetric(horizontal: 4, vertical: 4),
              padding: const EdgeInsets.symmetric(horizontal: 14),
              decoration: BoxDecoration(
                color: selected ? colorScheme.primaryContainer : null,
                borderRadius: BorderRadius.circular(12),
              ),
              child: Row(children: [
                const Icon(Icons.account_balance_wallet_outlined, size: 18),
                const SizedBox(width: 6),
                Text(a.name, style: const TextStyle(fontSize: 13)),
              ]),
            ),
          );
        },
      ),
    );
  }
}
```

```dart
// lib/presentation/screens/add_transaction_screen.dart
// import ../widgets/account_picker.dart
// state 加 int _accountId = -1；
// _autoSelectCategory 同款同步兜底：_autoSelectAccount(accounts)（第一个账户）
// build：AmountField 之后插 AccountPicker(selectedId: _accountId, onSelect: ...)
// _save：insertTransaction(..., accountId: _accountId)
```

- [ ] **Step 4: 运行确认通过**

Run: `flutter test test/presentation/add_transaction_screen_test.dart test/application/providers_test.dart`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add lib/application/providers.dart lib/presentation/widgets/account_picker.dart lib/presentation/screens/add_transaction_screen.dart test/presentation/add_transaction_screen_test.dart
git commit -m "feat: add account picker to transaction form"
```

---

### Task 5: 转账页 + 账户页 + 设置页入口

**Files:**
- Create: `lib/presentation/screens/transfer_screen.dart` + `test/presentation/transfer_screen_test.dart`
- Create: `lib/presentation/screens/accounts_screen.dart` + `test/presentation/accounts_screen_test.dart`
- Modify: `lib/presentation/screens/settings_screen.dart`（账户入口）+ `test/presentation/settings_screen_test.dart`

**Interfaces:**
- Consumes: Task 3/4 DAO 与 providers
- Produces: `class TransferScreen extends ConsumerWidget`、`class AccountsScreen extends ConsumerWidget`

- [ ] **Step 1: 写失败测试**

```dart
// test/presentation/transfer_screen_test.dart
// 内存 db：账户 1=现金、2=微信（seed 后手动 insert），无分类种子
// 测试 1 transfer saves and updates balances:
//   pump → 选从账户（默认 1）→ 到账户选 '微信' → 金额 '100' → 保存
//   DAO 断言 balanceOf(1) == -10000（无其他交易）、balanceOf(2) == 10000
// 测试 2 same account shows error:
//   从=到=现金（默认）→ 保存 → SnackBar '转出与转入账户不能相同'
// （均以 pumpWidget(SizedBox())+pump(Duration.zero) 收尾）

// test/presentation/accounts_screen_test.dart
// 测试 1 shows account list with balance（'现金' 与余额文本）
// 测试 2 adds account（FAB → 输入 '银行卡' → 确定 → 列表出现）
// 测试 3 delete account in use shows snackbar（有交易时删除 → '该账户已有记录，无法删除'）
// 测试 4 transfer button opens TransferScreen

// test/presentation/settings_screen_test.dart 追加：
// '账户' 入口 → AccountsScreen
```

（widget 测试代码结构与既有 settings/categories 测试同模式，执行时按此结构展开；余额断言用 AccountsDao(db).balanceOf 一次性查询。）

- [ ] **Step 2: 运行确认失败** → **Step 3: 实现**

```dart
// lib/presentation/screens/transfer_screen.dart（结构同 AddTransactionScreen）
class TransferScreen extends ConsumerStatefulWidget {
  // state: _fromId/_toId（默认第一/第二账户，同步兜底）、金额/日期/备注 controller
  // build: 从账户 AccountPicker、到账户 AccountPicker、AmountField、日期 ListTile、备注、保存
  // _save 校验：from == to → SnackBar '转出与转入账户不能相同'；parseCents；insertTransfer → pop
}

// lib/presentation/screens/accounts_screen.dart
class AccountsScreen extends ConsumerWidget {
  // 列表：CircleAvatar(Icons.account_balance_wallet_outlined) + 名称 + 余额（formatCents，
  //   负 AppTheme.expenseColor / 正 AppTheme.incomeColor，来自 FutureBuilder 或 stream 聚合——
  //   余额用 allTransactionsProvider 计算 accountBalances 一次，避免每行查询）
  // FAB：新增 dialog（名称 TextField，空名确定不生效）
  // AppBar action：转账图标 → TransferScreen
  // 删除：trailing delete IconButton → AccountInUseException → SnackBar '该账户已有记录，无法删除'
}

// lib/presentation/screens/settings_screen.dart ListView 追加（周期记账之后）：
        ListTile(
            leading: const Icon(Icons.account_balance_wallet_outlined),
            title: const Text('账户'),
            subtitle: const Text('现金/微信/银行卡等多账户与转账'),
            onTap: () => Navigator.push(context,
                MaterialPageRoute(builder: (_) => const AccountsScreen()))),
```

- [ ] **Step 4: 运行确认通过** → **Step 5: 提交**

```bash
git add lib/presentation/screens/transfer_screen.dart lib/presentation/screens/accounts_screen.dart test/presentation/transfer_screen_test.dart test/presentation/accounts_screen_test.dart lib/presentation/screens/settings_screen.dart test/presentation/settings_screen_test.dart
git commit -m "feat: add accounts and transfer screens"
```

---

### Task 6: 首页总资产 + 列表账户显示 + 周期规则账户

**Files:**
- Modify: `lib/presentation/screens/home_screen.dart` + `test/presentation/home_screen_test.dart`
- Modify: `lib/presentation/screens/transactions_screen.dart` + `test/presentation/transactions_screen_test.dart`
- Modify: `lib/presentation/screens/recurring_screen.dart` + `test/presentation/recurring_screen_test.dart`

- [ ] **Step 1: 写失败测试**

```dart
// home_screen_test 追加：
  testWidgets('shows total assets and opens accounts screen', (tester) async {
    // seed 无交易 → 总资产 0.00
    await pump... expect(find.textContaining('总资产'), findsOneWidget);
    await tester.tap(find.textContaining('总资产'));
    await tester.pumpAndSettle();
    expect(find.byType(AccountsScreen), findsOneWidget);
  });

// transactions_screen_test 追加：
  testWidgets('shows account name and transfer style', (tester) async {
    // 插一条 transfer：现金 → 微信（金额 500）
    await pump(tester);
    expect(find.textContaining('现金 → 微信'), findsOneWidget);
    expect(find.byIcon(Icons.swap_horiz), findsOneWidget);
  });

// recurring_screen_test 'adds a rule' 测试：dialog 内多一个 AccountPicker，
//  保存后规则行 subtitle 含账户名（'现金'）
```

- [ ] **Step 2: 运行确认失败** → **Step 3: 实现**

```dart
// home_screen.dart
// _SummaryCard 上方（Column 内、卡片前）：
        InkWell(
          onTap: () => Navigator.push(context,
              MaterialPageRoute(builder: (_) => const AccountsScreen())),
          child: Padding(
            padding: const EdgeInsets.fromLTRB(16, 12, 16, 0),
            child: Row(mainAxisAlignment: MainAxisAlignment.center, children: [
              const Icon(Icons.account_balance_wallet_outlined, size: 16),
              const SizedBox(width: 4),
              Text('总资产 ${formatCents(ref.watch(totalAssetsProvider))}',
                  style: const TextStyle(fontWeight: FontWeight.w600, fontSize: 16)),
            ]),
          ),
        ),

// transactions_screen.dart / home_screen tile
// 账户名 map：accountName(int id)；subtitle 改为：
//   转账：'${accountName(tx.accountId)} → ${accountName(tx.transferAccountId!)}'，leading 换
//   Icon(Icons.swap_horiz)（转账）否则 CircleAvatar 分类首字
//   普通：subtitle = '${accountName(tx.accountId)}${note 非空 ? ' · $note' : ''}'

// recurring_screen.dart
// dialog 加 AccountPicker（默认第一账户，同步兜底）；insertRule 加 accountId；
// 行 subtitle 加 ' · ${accountName(r.accountId)}'
```

- [ ] **Step 4: 运行确认通过** → **Step 5: 提交**

```bash
git add lib/presentation/screens/home_screen.dart lib/presentation/screens/transactions_screen.dart lib/presentation/screens/recurring_screen.dart test/presentation/home_screen_test.dart test/presentation/transactions_screen_test.dart test/presentation/recurring_screen_test.dart
git commit -m "feat: show total assets and account names in lists"
```

---

### Task 7: 备份/CSV 兼容 + README + 全量验证

**Files:**
- Modify: `lib/core/backup.dart` + `test/core/backup_test.dart`（accountId/transferAccountId 往返 + 旧备份缺省）
- Modify: `lib/core/csv_export.dart` + `test/core/csv_export_test.dart`（转账行 + account 列）
- Modify: `README.md`

- [ ] **Step 1: 写失败测试**

```dart
// backup_test：roundtrip 断言加 accountId（构造 _tx 加 accountId: 2）与 transferAccountId；
//   旧格式（无 accountId 字段）解析默认 1
// csv_export_test：
//   列头含 account：'date,type,category,account,amount,note'
//   转账行：'2026-08-13,转账,,现金,10.00,'（category 空、type 中文"转账"）
//   普通行：'2026-08-13,支出,餐饮,现金,12.34,午饭'
```

- [ ] **Step 2: 运行确认失败** → **Step 3: 实现**

```dart
// backup.dart encode 的 transaction map 加：
            'accountId': t.accountId,
            'transferAccountId': t.transferAccountId,
// _parseTransaction 加：
    accountId: m['accountId'] as int? ?? 1,
    transferAccountId: m['transferAccountId'] as int?,
// csv_export.dart：
// 列头 '﻿date,type,category,account,amount,note\r\n'
// 每行：type 列 → t.type == transfer ? '转账' : (expense ? '支出' : '收入')
//   category 列 → t.type == transfer ? '' : escapeCsv(catName[t.categoryId] ?? '未分类')
//   account 列 → accountName[t.accountId] ?? '未知账户'
// buildCsv 签名改：buildCsv(List<Transaction> txs, List<Category> cats, List<Account> accounts)
//   （调用方 settings_screen 同步传 all accounts —— exportAll 返回加 accounts 字段）
// backup_dao.exportAll 返回加 accounts：({categories, transactions, accounts})
```

```dart
// settings_screen exportCsv 调用处：
  await share('ledger-${todayYyyymmdd()}.csv',
      buildCsv(all.transactions, all.categories, all.accounts));
```

- [ ] **Step 4: README 功能清单追加**

```markdown
- **多账户**：现金/微信/支付宝/银行卡等多账户记账、账户间转账、总资产一目了然
```

- [ ] **Step 5: 全量验证**

Run: `flutter test && flutter analyze`
Expected: 全部 PASS（约 82 + 15 ≈ 97 tests）；analyze 零问题

- [ ] **Step 6: 提交**

```bash
git add lib/core/backup.dart lib/core/csv_export.dart lib/data/dao/backup_dao.dart lib/presentation/screens/settings_screen.dart test/core/backup_test.dart test/core/csv_export_test.dart README.md
git commit -m "feat: support accounts in backup and csv export"
```

---

## Self-Review 记录

- **Spec 覆盖**：表+migration（Task 1）、余额聚合（Task 2）、AccountsDao+转账插入（Task 3）、providers+记一笔选择器（Task 4）、转账页+账户页+设置入口（Task 5）、首页总资产+列表账户+周期规则账户（Task 6）、备份/CSV+README（Task 7）。
- **类型一致性**：`insertTransaction(..., accountId:)` 在 Task 1 定义、Task 3/4/6 使用一致；`insertTransfer({fromAccountId, toAccountId, amountCents, date, note})` Task 3 定义 Task 5 使用；`accountBalances/totalAssets` Task 2 定义 Task 3/4/5 使用；`buildCsv(txs, cats, accounts)` 新签名 Task 7 定义，settings_screen 同任务更新。
- **风险**：① Task 1 的 alterTable 重建 —— 若 drift 对 FK 重建报错，检查重建顺序（accounts 先建好）；② Task 5 转账测试里账户 2 需先 insert（无种子账户 2）；③ Task 6 home 总资产 InkWell 若与现有布局冲突（测试 800x1600 视口），保持 Column 结构。④ buildCsv 签名变化影响 settings_screen 调用 —— Task 7 内一并更新，不留跨任务编译断裂。
